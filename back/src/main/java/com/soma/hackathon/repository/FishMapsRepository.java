package com.soma.hackathon.repository;

import com.soma.hackathon.entity.FishMaps;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FishMapsRepository extends JpaRepository<FishMaps, Long> {
}
